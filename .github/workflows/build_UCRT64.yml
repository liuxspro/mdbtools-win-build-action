name: Build UCRT64
on:
  workflow_dispatch:

jobs:
  build-mdbtools-win:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - name: Setup MSYS2 UCRT64
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            autoconf
            automake
            libtool
            mingw-w64-ucrt-x86_64-toolchain

      - name: Checkout
        uses: actions/checkout@v4

      - name: Clone Mdbtools
        run: |
          git clone -b v1.0.0 --single-branch https://github.com/mdbtools/mdbtools.git

      - name: Debug
        run: |
          gcc -v
          ls
          pwd

      - name: Autoconf
        # 当前目录/d/a/mdbtools-win-build-action/mdbtools-win-build-action  又回到上一级目录了
        run: cd mdbtools && autoreconf -i -f

      - name: Configure
        run: cd mdbtools && ./configure

      - name: Make
        run: cd mdbtools && make all

      - name: Test
        run: |
          cd mdbtools
          ./src/util/mdb-export.exe --version
          ldd ./src/util/mdb-export.exe
